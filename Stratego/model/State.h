#ifndef STATE_H
#define STATE_H

/**
 * @brief The State enum represents the state of the game.
 */
enum State {
    NOT_STARTED,
    STARTED,
    BLUE_TURN,
    RED_TURN,
    ENDED
};

#endif // STATE_H
